<!DOCTYPE html>

<html>
<head>
    <%- include('./partials/head'); %>
</head>

<body>

<%- include('./partials/header'); %>
<div class="container">
    <h1>Comptoir de prêt</h1>
    <h2>Liste d'Administrateurs</h2>
    <table class="table table-striped table-hover">
        <tr>
            <th>id</th>
            <th>Nom</th>
            <th>email</th>
            <th>Action</th>
        </tr>
        <% adminitrateurs.forEach(function(adminitrateur) { %>
            <tr>
                <form name="form1" onsubmit="return validate_user()" action="adminChange" method="post">
                    <input id="<%= adminitrateur.NADMIN %>" type='hidden' name='radioAdmin'
                           value="<%= adminitrateur.NADMIN %>"/>
                    <td><%= adminitrateur.NADMIN %></td>
                    <td><%= adminitrateur.PRENOM %> <%= adminitrateur.NOM %></td>
                    <td><%= adminitrateur.EMAIL %></td>
                    <td>
                        <div class="form-inline">
                            <div class="form-group">
                                <div class="input-group">
                                    <select name="changement" class="form-control">
                                        <option value="delete">supprimer</option>
                                        <option value="Modifier">modifier</option>
                                        <option value="Modifier mot de passe">Modifier mot de passe</option>
                                    </select>
                                    <span class="input-group-btn">
                                            <input class="inputbutton" type="submit" name="b_submit"
                                                   value="Envoyer"/></span>
                                </div>
                            </div>
                        </div>
                    </td>
                </form>
            </tr>
        <% }); %>
    </table>
</div>
</body>
</html>
